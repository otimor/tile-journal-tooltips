<div class="tab" data-group="" data-tab="tile-journal-tooltips-fix">
  <div class="form-group">
    <label>Enable Tooltip</label>
    <input type="checkbox" name="flags.tile-journal-tooltips-fix.enabled" {{#if enabled}}checked{{/if}}>
    <p class="notes">
      When enabled, hovering this tile shows a tooltip near the cursor.
      Tooltip content is cached onto the tile when a GM saves, so players can see it without Journal permissions.
    </p>
  </div>

  <div class="form-group">
    <label>Journal Entry (source)</label>
    <select name="flags.tile-journal-tooltips-fix.journalId" class="tjt-journal">
      <option value="">— None —</option>
      {{#each journalOptions}}
        <option value="{{this.id}}" {{#if this.selected}}selected{{/if}}>{{this.name}}</option>
      {{/each}}
    </select>
    <p class="notes">Select the Journal Entry that provides the tooltip text (players do not require permissions; cached to tile).</p>
  </div>

  <div class="form-group">
    <label>Journal Page (source)</label>
    <select name="flags.tile-journal-tooltips-fix.pageId" class="tjt-page" {{#unless hasJournal}}disabled{{/unless}}>
      <option value="">— First Page / Entry Content —</option>
      {{#each pageOptions}}
        <option value="{{this.id}}" {{#if this.selected}}selected{{/if}}>{{this.name}}</option>
      {{/each}}
    </select>
    <p class="notes">
      If unset, the tooltip uses the first page (if pages exist) or the entry content.
      Changing the entry resets this to default.
    </p>
  </div>

  <hr/>

  <div class="form-group">
    <label>Cache Status</label>
    <div class="form-fields">
      <span class="tjt-cache-status">
        {{#if hasCache}}
          ✅ Cached (last updated: {{cacheUpdated}})
        {{else}}
          ⚠️ Not cached yet
        {{/if}}
      </span>
    </div>
    <p class="notes">
      Players see only cached content. If you update the Journal text, use the Cache Now button or re-save the tile to refresh it.
    </p>
  </div>

  <div class="form-group">
    <label></label>
    <div class="form-fields">
      <button type="button" class="tjt-cache-btn">
        <i class="fa-solid fa-rotate"></i> Cache Now
      </button>
    </div>
    <p class="notes">Caches the currently selected journal and page immediately, without requiring a full save.</p>
  </div>
</div>
